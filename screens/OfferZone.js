import * as React from 'react';
import { Button, View, Text, SafeAreaView } from 'react-native';

const OfferZone = ({ navigation }) => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <View style={{ flex: 1, padding: 16 }}>

        <Text>OfferZone</Text>

      </View>
    </SafeAreaView>
  );
};

export default OfferZone;